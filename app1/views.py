from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.
def jagadeesh(request):
    return HttpResponse('good boy')
def kohli(request):
    return HttpResponse('GOOD player')
